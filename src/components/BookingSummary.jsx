import { useState } from "react";

export function BookingSummary( { screening } ) {
  const [selectedTicketType, setSelectedTicketType] = useState(null);

  return (
    <>
        BookingSummary
    </>
  );
}